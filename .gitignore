.DS_Store
node_modules/
*.log
# Local Netlify folder
.netlify
# Personal documents — never commit
assets/*.pdf
# ESP32 firmware sketch (kept local only)
firmware/
# Local drive-link overrides (auth token — never commit)
js/config.local.js
